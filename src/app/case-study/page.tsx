import Link from "next/link";

export default function CaseStudyPage(){return <><header className="topbar"><div className="topbarInner"><Link className="brand" href="/"><span aria-hidden="true">&gt;_</span> reporeplay</Link><Link href="/repositories/demo">Open showcase</Link></div></header><main className="shell caseStudy" id="main-content"><p className="eyebrow">Engineering case study</p><h1>Evidence before interpretation.</h1><p className="lede">RepoReplay narrows its first release to public Next.js repositories so route and declared-dependency transitions can remain deterministic, complete, and traceable.</p><section><h2>Why first-parent history</h2><p>First-parent traversal produces a coherent mainline. Merge commits are compared with their first parent, which keeps transition semantics explicit without pretending to model the full Git graph.</p></section><section><h2>Why durable processing</h2><p>Import work runs outside HTTP requests. PostgreSQL leases, checkpoints, retries, and staged activation keep the last successful snapshot readable when a worker stops or a refresh fails.</p></section><section><h2>Why fixture-first UI</h2><p>The frontend consumes typed API-shaped fixtures. Backend implementation can replace the fixture service one endpoint at a time without moving state logic into presentation components.</p></section></main></>}
